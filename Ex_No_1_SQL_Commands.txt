
-- Ex. No. 1 : WORLD DATABASE (SQL Commands)

SELECT name,population FROM country WHERE name='Germany';

SELECT name, population FROM country WHERE name IN ('Ireland','Iceland','Denmark');

SELECT name, surfacearea FROM country WHERE surfacearea BETWEEN 250000 AND 300000;

SELECT SUM(population) FROM country;

SELECT DISTINCT continent FROM country;

SELECT COUNT(name) FROM country WHERE surfacearea > 1000000;

SELECT SUM(population) FROM country WHERE name IN ('France','Germany','Spain');

SELECT name FROM country WHERE name LIKE 'z%a';

SELECT name FROM country WHERE name LIKE '%United%';

SELECT name FROM country WHERE name LIKE '____';

SELECT name FROM country WHERE population >= 200000000;

SELECT name, population FROM country WHERE continent='Asia' ORDER BY population LIMIT 10;

SELECT name, population/surfacearea FROM country WHERE population >= 200000000;

SELECT name, population/1000000 AS 'Population in Million' FROM country WHERE continent='South America';

SELECT name,population,surfacearea FROM country 
WHERE surfacearea > 3000000 OR population > 250000000;

SELECT name,population,surfacearea FROM country 
WHERE (surfacearea > 3000000 OR population > 250000000)
AND NOT (surfacearea > 3000000 AND population > 250000000);

SELECT name,population FROM country WHERE population > 100000000 ORDER BY population DESC;

SELECT name,population FROM city WHERE countrycode='IND' ORDER BY population DESC;

SELECT name,indepyear FROM country WHERE indepyear IS NULL;

SELECT *,
CASE 
WHEN percentage <= 10 THEN 'Low'
WHEN percentage BETWEEN 11 AND 50 THEN 'Medium'
ELSE 'High'
END
FROM countrylanguage;

SELECT COUNT(DISTINCT language) FROM countrylanguage;

SELECT code,name,indepyear FROM country 
WHERE indepyear IS NOT NULL ORDER BY indepyear DESC LIMIT 1;

SELECT DISTINCT language FROM countrylanguage 
WHERE isofficial='T' ORDER BY language DESC;

SELECT name FROM country WHERE name LIKE '%Republic%';

SELECT name FROM country WHERE continent <> 'Africa';

SELECT name,population FROM country ORDER BY population DESC LIMIT 3;

SELECT continent,name,population FROM country 
WHERE continent='Europe' ORDER BY population DESC LIMIT 1;

SELECT name,surfacearea FROM country ORDER BY surfacearea LIMIT 1;

SELECT continent,name,surfacearea FROM country 
WHERE continent='Asia' ORDER BY surfacearea DESC LIMIT 1;

SELECT continent,name,population/surfacearea FROM country 
WHERE continent='Asia' ORDER BY 3 DESC;

SELECT continent,name,gnp/population FROM country ORDER BY gnp/population DESC;

SELECT name FROM country WHERE LEFT(name,1)=RIGHT(name,1);

SELECT GROUP_CONCAT(name ORDER BY name SEPARATOR '-') 
FROM city WHERE countrycode='IND' AND district='Tamil Nadu';

SELECT name,lifeexpectancy FROM country ORDER BY lifeexpectancy DESC LIMIT 1;

SELECT name,population FROM city 
WHERE population=(SELECT MAX(population) FROM city);

SELECT name,population FROM country 
WHERE population >= ALL (SELECT population FROM country);

SELECT name,population FROM country 
WHERE population > (SELECT population FROM country WHERE name='Romania');

SELECT name,continent FROM country 
WHERE continent=(SELECT continent FROM country WHERE name='Brazil');

SELECT name,continent FROM country 
WHERE continent IN (
SELECT continent FROM country WHERE name IN ('Brazil','Mexico')
);

SELECT name,population/(SELECT population FROM country WHERE name='United Kingdom') 
FROM country WHERE name='China';

SELECT name,population FROM country 
WHERE population > ALL (SELECT population FROM country WHERE continent='Europe');

SELECT name,
CONCAT(ROUND((population/(SELECT population FROM country WHERE name='Germany')*100),0),'%')
FROM country WHERE continent='Europe';

SELECT name,population FROM country 
WHERE population > (SELECT population FROM country WHERE name='Canada')
AND population < (SELECT population FROM country WHERE name='Poland');

SELECT language FROM countrylanguage 
WHERE countrycode=(SELECT code FROM country WHERE name='Finland');

SELECT continent,name,population FROM country 
WHERE continent='South America' ORDER BY population LIMIT 1;

SELECT continent,name,surfacearea FROM country x
WHERE surfacearea >= ALL (
SELECT surfacearea FROM country y 
WHERE y.continent=x.continent AND surfacearea > 0
);

SELECT continent,name,population FROM country t1
WHERE population=(
SELECT MAX(population) FROM country t2 
WHERE t1.continent=t2.continent
);

SELECT CONCAT('The country code for Finland is:',
(SELECT code FROM country WHERE name='Finland'));

SELECT (SELECT SUM(population) FROM city) /
(SELECT SUM(population) FROM country) AS ratio;

SELECT DISTINCT continent FROM country 
WHERE code IN (
SELECT countrycode FROM countrylanguage 
WHERE language='English' AND percentage > 50
);

SELECT name FROM country WHERE continent='Europe'
AND code IN (
SELECT countrycode FROM countrylanguage WHERE language='Spanish'
);
